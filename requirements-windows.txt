# Windows-specific extras for EchoLocate
# openwakeword's own installer auto-selects PyAudioWPatch on win32,
# but listing it here makes the dependency explicit and auditable.
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-windows.txt

PyAudioWPatch==0.2.12.8
pywin32>=306
